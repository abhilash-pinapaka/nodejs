// Primitive Types

// String, Number, boolean, undefined, null

let courseName = "JavaScript";
let couseLength = 30;
let isApproved = true;
let firstName = undefined;
let selectColor = null;

// Dynamic Typing
// Static languages = type of variable is said and cannot be changed later
// Dynamic Language type of variable can change at runtime

typeof(isApproved);
isApproved = "yes";
typeof(isApproved);
typeof(firstName);
// undefined is actually a type and also a value;
typeof(selectColor);