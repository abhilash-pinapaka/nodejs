let name;
console.log(name);
// by default variable value is undefined
let course = "JavaScript";
console.log(course);
// variable name should not keywords
// variables should be meaningful
// cannot start with a number
// cannot contain space or -
// case sensitive

// Declaring multiple names
let courseName="JavaScript"; courseType="node";


// Constant
let rate = 0.5;
rate = 1;
console.log(rate);

// value of const cannot change

const fixedRate=0.5;
fixedRate = 1;
console.log(fixedRate);

